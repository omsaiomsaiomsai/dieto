[server]
headless = true
address = "0.0.0.0"
port = 5000

[theme]
# Permanent light mode settings
base = "light"
primaryColor = "#4CAF50"  # Green primary color
backgroundColor = "#FFFFFF"  # White background for main content
secondaryBackgroundColor = "#F0F8FF"  # Light blue for sidebar/card backgrounds
textColor = "#262730"  # Dark text for better readability
font = "sans serif"